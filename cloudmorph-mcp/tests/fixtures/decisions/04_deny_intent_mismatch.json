{
  "name": "deny_intent_mismatch",
  "description": "Agent declared write.delete intent; attempting read.list still denies via mismatch verdict (the matcher returns mismatch when declared verbs don't include the action's required verbs).",
  "input": {
    "toolCall": {"action": "aws.s3.list_buckets", "arguments": {}},
    "tenantSettings": {"locked": false},
    "intent": {
      "intentId": "int_test_xxx",
      "structuredVerbs": ["write.delete"]
    },
    "intentMatchScore": {
      "verdict": "mismatch",
      "confidence": 0.0,
      "stage": "lexical"
    }
  },
  "expected": {
    "outcome": "deny",
    "reason": "intent_mismatch"
  }
}
