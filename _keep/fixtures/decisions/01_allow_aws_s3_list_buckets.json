{
  "name": "allow_aws_s3_list_buckets",
  "description": "AWS S3 list_buckets is in the readonly allowlist; allow expected.",
  "input": {
    "toolCall": {"action": "aws.s3.list_buckets", "arguments": {}},
    "tenantSettings": {"locked": false}
  },
  "expected": {
    "outcome": "allow",
    "reason": "allow_read_first"
  }
}
